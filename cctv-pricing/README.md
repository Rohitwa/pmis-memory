# Store Monitoring — Pricing Site

Static pricing page (5 phases, YantrAI/Baxy split, model toggle).
Deploy from this directory: `fly deploy` (app: store-monitoring-pricing, region bom),
or via the GitHub Actions workflow once FLY_API_TOKEN secret is set.
