# PitchGraph — Gemini CLI project instructions

The full agent instruction lives in `AGENTS.md` so that Codex, Claude Code, and Gemini CLI all
run the same agent from one file. Load it:

@./AGENTS.md

If your Gemini client does not expand the `@./AGENTS.md` line above, read `AGENTS.md` in this
folder now and follow it exactly. Start every new session by greeting the user with `help`
if no `strategy/product_profile.md` exists yet.
